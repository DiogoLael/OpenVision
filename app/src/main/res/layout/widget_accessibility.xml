<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/widget_background"
    android:orientation="horizontal"
    android:padding="8dp">

    <ImageButton
        android:id="@+id/btn_daltonism"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:layout_weight="1"
        android:background="?android:attr/selectableItemBackground"
        android:src="@drawable/ic_daltonism"
        android:contentDescription="Filtro Daltonismo"
        android:layout_margin="4dp" />

    <ImageButton
        android:id="@+id/btn_talkback"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:layout_weight="1"
        android:background="?android:attr/selectableItemBackground"
        android:src="@drawable/ic_talkback"
        android:contentDescription="TalkBack"
        android:layout_margin="4dp" />

    <ImageButton
        android:id="@+id/btn_translate"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:layout_weight="1"
        android:background="?android:attr/selectableItemBackground"
        android:src="@drawable/ic_translate"
        android:contentDescription="Tradutor"
        android:layout_margin="4dp" />

</LinearLayout>